<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="WeatherForecastLarge" parent="AppTheme">
       <item name="android:textSize">@dimen/style_weather_large_font</item>
    </style>

    <style name="WeatherForecastMed" parent="AppTheme">
        <item name="android:textSize">@dimen/style_weather_medium_font</item>
    </style>
    <style name="MyTimePickerDialogStyle" parent="@android:style/Widget.Material.TimePicker">
        <item name="android:headerBackground">#ffe082</item>
        <item name="android:timePickerMode">spinner</item>
    </style>
    <style name="DetailDisplay" parent="AppTheme">
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>

    </style>



</resources>
